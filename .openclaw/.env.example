# OpenClaw environment variables for the hypergraph dev team setup.
# Copy this file to .openclaw/.env and fill in your values.
# NEVER commit .openclaw/.env to git.

# ─── Gateway security ─────────────────────────────────────────────────────────
# Generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=change-me-to-a-long-random-token

# ─── Model provider API keys ──────────────────────────────────────────────────
# At minimum, set ANTHROPIC_API_KEY (for Zoe + Claude-Planner).
# Set OPENAI_API_KEY for Codex-Agent (best code correctness).
# Set GEMINI_API_KEY for Gemini-Reviewer (free, 1M context).

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=...

# ─── Notification channel (Telegram recommended) ──────────────────────────────
# Create a bot at https://t.me/BotFather, then get your chat ID.
TELEGRAM_BOT_TOKEN=123456:ABCDEF...
# TELEGRAM_CHAT_ID=your-chat-id  (used by the notification scripts)

# ─── GitHub (for PR notifications and CI status checks) ───────────────────────
# gh CLI must be authenticated: `gh auth login`
# These are used by the monitoring cron script.
GITHUB_REPO=gilad-rubin/hypergraph
